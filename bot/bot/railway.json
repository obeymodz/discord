{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm start",

    "// healthcheckPath": "GET / returns the plain text 'aurora relay'. Railway holds the new deployment back until this responds, so a broken build never replaces a working one.",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,

    "// restartPolicy": "Discord drops gateway connections routinely (deploys, network blips, Discord-side restarts). discord.js reconnects on its own, but if the process itself dies we want it back without manual intervention.",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
